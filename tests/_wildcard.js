const { Chance, CommunityChest } = require('../core/wildcard');

test('CHANCE CARD TEST -->', () => {
  test('', () => {

  });
});

test('COMMUNITY CHEST TEST -->', () => {
  test('', () => {

  });
});

const canvas = document.getElementById("drawing-canvas");

const drawing_sheet = canvas.getContext("2d");

drawing_sheet.drawImage(
  // source of the image
  // dimensions(to be written) of the source... x,y,height,width
  // dimensions(to be written) on the destination... x,y,height,width
)